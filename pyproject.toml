[project]
name = "etui"
version = "0.0.2"
description = "CLI Scriptlauncher in a simple TUI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ruff>=0.14.10",
    "textual>=7.0.0",
]

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
]

[tool.uv]
package = true
